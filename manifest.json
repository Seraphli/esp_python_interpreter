{
  "name": "ES Python Interpreter",
  "version": "",
  "description": "Run one line python code in ES",
  "init": {
    "default": [
      "PYTHON -m pip install -r requirements.txt --disable-pip-version-check"
    ]
  },
  "run": {
    "default": "PYTHON plugin.py"
  }
}
